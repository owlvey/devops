spec:
  template:
    spec:
      containers:
      - name: tcpflowcontainer
        image: byfcz/tcpflow
        securityContext:          
          allowPrivilegeEscalation: false          
        args:
            - -oapp # -o app
            - -ehttp
      

